#include<bits/stdc++.h>
using namespace std;   

int main(){
    vector<int>v={1,3,2,4,3,4,1,6};   
    int x=7,y,z;
    for(int i=0;i<v.size()-1;i++){
        y=v[i];
        for(int j=i+1;j<v.size();j++){
            z=v[j];
            if(x==(y+z)) cout<<i<<","<<j<<endl;
        }
    }
} 

