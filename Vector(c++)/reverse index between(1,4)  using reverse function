#include<bits/stdc++.h>
using namespace std;   

int main(){
    vector<int>v1={1,3,2,4,6,8,9,11};   
    int x=1,y=4;
    reverse(v1.begin()+x,v1.begin()+y+1);
    for(int i=0;i<v1.size();i++){
        cout<<v1[i]<<" ";
    }
} 
