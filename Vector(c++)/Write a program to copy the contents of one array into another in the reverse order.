#include<bits/stdc++.h>
using namespace std;   

int main(){
    vector<int>v1={1,3,2,4,3,4,1,6};   
    vector<int>v2(v1.size());
    for(int i=v1.size()-1;i>=0;i--){
        v2[v1.size()-1-i]=v1[i];
    }

    for(int i=0;i<v2.size();i++){
        cout<<v2[i]<<" ";
    }
} 
