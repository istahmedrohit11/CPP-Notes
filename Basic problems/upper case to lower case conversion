#include<iostream>
#include <cctype>
using namespace std;
int main(){
  char ch;
  cout<<"Enter character: ";
  cin>>ch;
  if(ch>='A' && ch<='Z'){
   char uppercase=tolower(ch);
   cout<<"Upper letter of "<<ch<<" is :"<<uppercase;
  }else{
    cout<<"please enter a lower letter.";
  }
}
