// Output:
// 1 2 3 
// 4 5 6 
// 7 8 9 






























// Code Explanation:
// - The program prints a 3x3 number pattern using nested loops.
// - The variable `n` is initialized to 0 before the loops start.
// - The outer loop (`i`) runs 3 times (0 to 2), controlling the rows.
// - The inner loop (`j`) runs 3 times (0 to 2), printing `n + 1` and then incrementing `n`.
// - `cout << n + 1 << " ";` prints numbers starting from 1, as `n` is initially 0.
// - Since `n` is incremented after every print, it increases continuously across rows rather than resetting for each row.
// - `cout << endl;` moves to the next line after printing 3 numbers in a row.

#include <iostream>
using namespace std;
int main() {
    int i, j, n = 0; // Initialize n to 0
    for (i = 0; i < 3; i++) { // Outer loop for rows
        for (j = 0; j < 3; j++) { // Inner loop for columns
            cout << n + 1 << " "; // Print current value of n + 1
            n++; // Increment n
        }
        cout << endl; // Move to the next line
    }
}
