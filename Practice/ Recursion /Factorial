#include<iostream>
using namespace std;
int factorial(int x){
    if(x==0) return 1;
    return x * factorial(x-1);
}
int main(){
    int x;
    cout<<"Enter a number:";
    cin>>x;
    cout<<"The factorial of "<<x<<"is: "<<factorial(x);

}
