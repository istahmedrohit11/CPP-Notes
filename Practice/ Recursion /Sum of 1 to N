#include<iostream>
using namespace std;

int sumOfN(int n){
    if(n==0) return 0;
    return n + sumOfN(n-1);
}

int main(){
    int n;
    cout<<"Enter range:";
    cin>>n;
    cout<<"The sum of 1 to "<<n<<" is:"<<sumOfN(n);
}
