#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
   int arr[6]={22,94,90,85,66,45};

   int max=INT_MIN; 
   int Smax=INT_MIN;

   for(int i=0;i<6;i++){
    if(max<arr[i]){
        max=arr[i];
    }
   }
   for(int i=0;i<6;i++){
    if(arr[i]!=max && Smax<arr[i]){
        Smax=arr[i];
    }
   }

   cout<<max<<endl;
   cout<<Smax<<endl;
}  
